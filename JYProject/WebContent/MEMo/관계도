2-1. 관계
	- 학과와 학생은 1:다 관계
		학과는 많은 학생을 가질 수 있다.
		학생은 한 학과의 소속된다
	-  학과와 교수는 1:다 관계
		학과는 많은 교수를 가질 수 있다.
		교수는 한 한과의 소속된다.
	-  교수와 개설과목은 1:다 관계
		교수는 많은 과목를 가르칠 수 있다.
		과목은 강의하는 교수 한 명이 지정된다.
	-	과목과 학생은 다:다 관계
		과목은 수강하는 많은 학생을 가진다.
		학생은 많은 과목을 수강할 수 있다.

	관계형 테이터베이스는 다:다 관계를 허용하지 않는다.
	과목과 학생의 다:다 관계를 해소하기 위해서 조인테이블 역할을 수행할 수강 엔티티를 추가한다.
	
	- 과목과 수강은 1:다 관계
	- 학생과 수강은 1:다 관계
	
	-----------------------------------------------
	
	ㅁ 멤버와 워크스페이스는 다:다 관계
		- 멤버는 여러 워크스페이스에 소속될 수 있다.
		- 워크스페이스에 많은 멤버가 소속될 수 있다.
	ㅁ 멤버와 프로젝트는 다:다 관계
		- 멤버는 여러 프로젝트에 소속될 수 있다.
		- 프로젝트에 많은 멤버가 소속될 수 있다.
	ㅁ 멤버와 업무리스트는 다:다 관계
		- 멤버는 여러 업무리스트를 생성할 수 있다.
		- 업무리스트는 많은 멤버가 생성할 수 있다. 
	ㅁ 멤버와 업무는 다:다 관계
		- 멤버는 여러 업무를 담당할 수 있다.
		- 업무에는 많은 멤버가 담당할 수 있다.
	
	ㅁ 워크스페이스와 프로젝트는 1:다 관계
		- 워크스페이스에 여러 프로젝트를 생성할  수 있다.
		- 프로젝트는 하나의 워크스페이스에 생성할 수 있다.
	ㅁ 워크스페이스와 업무리스트 다:다 관계
		- 워크스페이스에 여러 업무리스트를 생성할  수 있다.
		- 업무리스트는  하나의 워크스페이스에 생성할 수 있다.
	ㅁ 워크스페이스와 업무 다:다 관계
		- 워크스페이스에 여러 업무를 생성할 수 있다.
		- 업무는 하나의 워크스페이스에 생성할 수 있다.
	
	ㅁ 프로젝트와 업무리스트는 1:다 관계
		- 프로젝트에 여러 업무리스트를 생성할  수 있다.
		- 업무리스트는 하나의 프로젝트에 생성할 수 있다.
	ㅁ 프로젝트와 업무는 1:다 관계
		- 프로젝트에 여러 업무를 생성할  수 있다.
		- 업무는 하나의 프로젝트에 생성할 수 있다.	
		
	ㅁ 업무리스트와 업무는 1:다 관계
		- 업무리스트에 여러 업무를 생성할  수 있다.
		- 업무는 하나의 업무리스트에 생성할 수 있다.	
		
		
	-----------------------------------------
		

		
		
		
		
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	